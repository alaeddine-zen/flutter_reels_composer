## 0.2.0

- Add `FlutterReelsComposerGpl.open` backed by x264-enabled FFmpeg.
- Isolated from the MIT facade; do not combine the two in one app.
- Does not re-export `CameraPage` / `GalleryPage` / `EditorPage` (use
  `package:flutter_reels_composer_ui/pages.dart`).

# Screenshot placeholders

Add product images here and reference them from the root README:

| File | Shot |
| --- | --- |
| `camera.png` | Camera / record |
| `editor.png` | Editor with tool rail |
| `export.png` | Export / result |

Suggested size: 1080×1920 (or a cropped 9:16 frame). WebP is fine.

This directory is empty on purpose until maintainers record a device demo.
Do not commit mockups that are not the real composer UI.
